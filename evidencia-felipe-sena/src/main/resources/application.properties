# Configuración JDBC (SQLite por defecto)
jdbc.url=jdbc:sqlite:./data/evidencia.db
jdbc.username=
jdbc.password=

# Auto-ejecución del esquema SQL al inicio (usado manualmente desde Main)
schema.path=/schema.sql
